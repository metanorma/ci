# Thin caller stub for metanorma/ci generic-automerge.yml (ci#379).
# Triggers stay here (reusable structural requirement). Body is the
# shared reusable — one place to bump pascalgn/automerge-action.

name: automerge

on:
  pull_request:
    types:
      - labeled
      - unlabeled
      - synchronize
      - opened
      - edited
      - ready_for_review
      - reopened
      - unlocked
  pull_request_review:
    types:
      - submitted
  check_suite:
    types:
      - completed
  status: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: metanorma/ci/.github/workflows/generic-automerge.yml@main
    secrets: inherit
